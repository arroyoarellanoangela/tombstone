name: Business Software Group
slug: bsg
allowed_domains:
  - bsg.site
primary_language: es
source_type: press_room
notes: >
  Spanish-language source. Values and quotes are extracted, verified, and
  displayed in Spanish end-to-end — no translation step exists anywhere in
  the pipeline, so the Verifier always matches against the actual page text
  and nothing can drift between the quote and what's shown (see
  agents/verifier.py).
